#include<iostream>
#include<cmath>
using namespace std;

int main() {
    long int n,result=0;
    cin>>n;
    int i=0;
    
    while(n>0)
    {
        int t=n%10;
        
        
        result=result+t*pow(2,i);
       n=n/10;
        i++;
    }
    cout<<result;
	
}
